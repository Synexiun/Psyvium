# Normalize line endings to LF in the repo for all text files (cross-platform).
* text=auto eol=lf

# Shell scripts must stay LF or they break under bash on checkout.
*.sh text eol=lf

# Lockfiles / generated — treat as text, LF.
pnpm-lock.yaml text eol=lf
